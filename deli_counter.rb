# Write your code here.

def line(array)
  if array.length >= 1
    new_array = []
    counter = 1
    array.each do |name|
      new_array.push("the line is currently #{counter}.#{name}")
      counter += 1
    end
    else
PUT
end
